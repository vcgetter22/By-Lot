# By Lot. — local environment variables.
# Copy this file to .env.local and fill in real values.
# Both variables must be prefixed with VITE_ so Vite exposes them to the client bundle.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
